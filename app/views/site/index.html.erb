<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Earthquake App</title>
  <link rel="stylesheet" href="">
</head>
<body>
  <% if user_signed_in? %>

    <p>Checklist:</p>
    <% @checklist_items.each do |c| %>
      <div>
        <%= c.description %>
        <%= link_to 'Delete', checklist_item_path(c),
            method: :delete %>
      </div>
    <% end %>

    <p>Add an item to your checklist:</p>

    <%= form_for :checklist_item, url: checklist_items_path do |f|  %>
      <div class="field">
        <%= f.label :description %><br>
        <%= f.text_field :description %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>

  <% else %>
    <p>Please login to access your checklist.</p>
  <% end %>
</body>
</html>


